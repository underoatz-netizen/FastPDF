// FastPDF Win32 resource identifiers.
//
// Shared between the compiled resource script (app.rc) and the C++ window
// code so the same numeric ID references the embedded application icon.
#ifndef FASTPDF_APP_RESOURCE_H
#define FASTPDF_APP_RESOURCE_H

// Application icon (also used as the window class / title-bar / task-bar icon).
#define IDI_APP_ICON 101

#endif // FASTPDF_APP_RESOURCE_H
