# บันทึกการเผยแพร่ FastPDF

[English](RELEASE_NOTES.md) · [ไทย](RELEASE_NOTES.th.md) — [README](README.th.md)

ข้อความเป็นบันทึกการเผยแพร่ของ FastPDF แต่ละเวอร์ชัน เขียนจากสิ่งที่ repository นี้รองรับจริง หัวข้อสุดท้ายเป็นข้อความพร้อมคัดลอกไปวางในช่องคำอธิบายของ GitHub Release

## สถานะไฟล์ที่เผยแพร่

| | |
| --- | --- |
| เวอร์ชันใน repository | `0.2.0` (กำหนดใน `CMakeLists.txt` และฝังเป็นเวอร์ชันของไฟล์/ผลิตภัณฑ์) |
| GitHub Release ที่เผยแพร่แล้ว | **ไม่มี** repository ยังไม่มี Release ที่เผยแพร่และไม่มี tag ใด ๆ |
| ไฟล์แนบ (asset) ที่เผยแพร่แล้ว | **ไม่มี** ไม่มีการแนบไฟล์ binary และ `dist/` ถูกยกเว้นไว้ใน [`.gitignore`](.gitignore) |
| ไฟล์แนบที่คาดว่าจะมีเมื่อสร้าง Release | `FastPDF-0.2.0-win-x64.zip` ซึ่ง build ในเครื่องด้วย [`scripts/package_release.ps1`](scripts/package_release.ps1) หรือ `cmake --build --preset release --target fastpdf_package_release` |
| ชื่อ tag ที่เสนอไว้ | `v0.2.0` (ยังเป็นการวางแผนไว้เท่านั้น ยังไม่ได้สร้าง) |

ข้อความทั้งหมดข้างล่างจึงเป็น **ร่างบันทึกการเผยแพร่** ข้อเท็จจริงเกี่ยวกับตัวโปรแกรมมีหลักฐานรองรับใน repository ส่วนไฟล์แนบจะยังไม่มีอยู่บน GitHub จนกว่าจะมีผู้ build
สร้าง Release และแนบไฟล์ ZIP เข้าไป

## มีอะไรใหม่ใน 0.2.0

เวอร์ชันนี้เป็น minor release ต่อจาก `0.1.1` เพิ่มงานส่วนติดต่อแบบทันสมัย (Modern UI) และการรองรับ Per-Monitor DPI ที่ทำเสร็จแล้ว ท่าทางดับเบิลคลิกเพื่อ Fit Page และการเปิดจาก command line ตามขอบเขตของ release โดยไม่เปลี่ยน dependency หรือขอบเขตผลิตภัณฑ์

- **ส่วนติดต่อแบบทันสมัย (Modern UI)** — ไฟล์โปรแกรมเลือกใช้ visual styles ของ Windows common controls v6 ทำให้เมนู ช่องแก้ไข/ปุ่มของแผงค้นหา และแถบเลื่อนวาดตามธีม Windows ปัจจุบัน พร้อมสถานะ hover / focus / pressed ที่มีมาให้ ถือเป็นองค์ประกอบของระบบปฏิบัติการ ไม่ใช่ dependency ใหม่
- **การรองรับ Per-Monitor DPI ที่สมบูรณ์** — แผงค้นหาและคอนโทรลลูกปรับขนาดและย้ายตำแหน่งใหม่ทันทีเมื่อหน้าต่างย้ายไปจอที่ DPI ต่างกัน ฟอนต์ของคอนโทรลถูกสร้างใหม่เฉพาะเมื่อความสูงเปลี่ยนจริง และแถบสถานะถูกสร้างใหม่ตาม DPI ใหม่ แถบสถานะแบ่งเป็นสองระดับชัดเจน โดยชื่อเอกสารอยู่ซ้าย ส่วนเลขหน้า/ซูม (และข้อมูลวินิจฉัยเมื่อเปิดใช้) ชิดขวาด้วยสีที่หม่นลง
- **ดับเบิลคลิกที่หน้าเพื่อ Fit Page** — ดับเบิลคลิกบนเนื้อหาหน้าที่เรนเดอร์แล้วจะย่อหน้านั้นให้พอดีกับกรอบมองโดยใช้คำสั่ง Fit Page เดิม (`Ctrl+0`) ส่วนการดับเบิลคลิกที่ขอบข้างหน้า ในช่องว่างระหว่างหน้า ในโหมดนำเสนอ หรือขณะจับภาพ จะไม่ทำอะไร
- **การเปิดจาก command line / file association ที่ยืนยันแล้ว** — `FastPDF.exe "C:\path\to\file.pdf"` เปิดเอกสารโดยตรง ปัจจุบันมี end-to-end test จำลองรูปแบบคำสั่ง `shell\open\command` ของ Explorer จริง ทั้งพาธไฟล์โปรแกรมที่มีช่องว่าง และพาธเอกสารที่มีช่องว่างและอักษรไทย Unicode โดยพาธไม่ถูกตัดแบ่งและตัวโปรแกรมเองไม่ถูกเปิดเป็นเอกสาร

## สรุปเวอร์ชัน 0.2.0

- **คืออะไร:** โปรแกรมอ่าน PDF ขนาดเบา รวดเร็ว และใช้ส่วนติดต่อขั้นต่ำสำหรับ Windows — สร้างด้วย Win32 + Direct2D + PDFium อ่านเอกสารต่อเนื่องหนึ่งคอลัมน์ และเรนเดอร์นอกเธรด UI
- **แพลตฟอร์ม:** Windows 10 / 11 **เฉพาะ x64** ต้องมี `pdfium.dll` อยู่ข้าง `FastPDF.exe` ไม่มีตัวติดตั้งและไม่ต้องการสิทธิ์ผู้ดูแลระบบ
- **ความสามารถหลัก:** การอ่านและซูม, โหมดนำเสนอ, จับภาพหน้าเอกสาร, แปลง PDF เป็น PNG, แปลงภาพเป็น PDF, พิมพ์แบบ native, ค้นหาข้อความแบบทันที, ไฟล์ที่เปิดล่าสุด, การเปิด PDF โดยตรงจาก command line, แผงข้อมูลวินิจฉัยแบบเลือกเปิดได้ และไอคอนแอปพลิเคชันที่ฝังในไฟล์โปรแกรม
- **สิ่งที่ยังไม่มีในเวอร์ชันนี้:** กล่องกรอกรหัสผ่าน, OCR, การรวม PDFium ไว้ใน source repository และไฟล์ `LICENSE` ที่ root
- **สถานะการทดสอบ:** CTest ผ่าน 24 รายการเมื่อเปิดใช้ PDFium (ทั้งแบบ Debug และ Release) และลงทะเบียน 15 รายการเมื่อไม่เปิดใช้ เป็นผลที่ผู้ดูแลบันทึกไว้ repository นี้ไม่มี workflow CI

## ข้อความสำหรับคัดลอกลง GitHub Release

- **tag** ที่เสนอ: `v0.2.0` (สร้างบน commit ที่จะเผยแพร่)
- **ชื่อ Release** ที่เสนอ: `FastPDF 0.2.0 (Windows x64)`

วางบล็อกด้านล่างลงในช่องคำอธิบายของ Release ข้อความนี้ใช้ลิงก์แบบ absolute จึงแสดงผลได้ถูกต้องแม้แสดงอยู่นอกมุมมองไฟล์ของ repository และใช้ code block แบบเยื้องซ้ายเพื่อให้คัดลอกได้ทั้งบล็อกโดยไม่ต้องแก้สิ่งใด ยกเว้นส่วนที่อธิบายไฟล์แนบที่คุณไม่ได้แนบจริง

```markdown
**FastPDF 0.2.0** เป็น minor update ของโปรแกรมอ่าน PDF ขนาดเบา รวดเร็วและใช้ส่วนติดต่อขั้นต่ำสำหรับ Windows ซึ่งสร้างด้วย Win32 + Direct2D + PDFium โดยเพิ่มส่วนติดต่อแบบทันสมัย การจัดการ Per-Monitor DPI ที่สมบูรณ์ การดับเบิลคลิกเพื่อ Fit Page และการเปิดจาก command line / file association ที่ยืนยันแล้ว เป็นเวอร์ชันต่อจาก 0.1.1

> **ไฟล์แนบ (Assets):** แนบ `FastPDF-0.2.0-win-x64.zip` ก็ต่อเมื่อไฟล์นั้นถูกสร้างในเครื่องด้วย
> [`scripts/package_release.ps1`](https://github.com/underoatz-netizen/FastPDF/blob/master/scripts/package_release.ps1)
> แล้วเท่านั้น หาก Release นี้ไม่มีไฟล์แนบ FastPDF จะเผยแพร่เฉพาะซอร์สโค้ด ให้ดูขั้นตอน build ใน
> [README](https://github.com/underoatz-netizen/FastPDF/blob/master/README.th.md)

## แพลตฟอร์มที่รองรับ

- Windows 10 หรือ Windows 11 แบบ **64 บิต (x64)** เท่านั้น
- `FastPDF.exe` ต้องอยู่ในโฟลเดอร์เดียวกับ `pdfium.dll` (ZIP แบบพกพาที่แนบไว้มีทั้งสองไฟล์อยู่แล้ว)
- ไม่ต้องใช้ตัวติดตั้ง ไม่ต้องใช้สิทธิ์ผู้ดูแลระบบ และไม่ต้องมี .NET runtime
  ส่วนติดต่อ (เมนู กล่องโต้ตอบ และข้อความต่าง ๆ) มีเฉพาะภาษาอังกฤษ

## วิธีรันแพ็กเกจแบบพกพา

1. ดาวน์โหลดและแตก ZIP ไปไว้ที่ใดก็ได้ เช่น `C:\Tools\FastPDF\`
2. เรียก `FastPDF.exe` ได้ทันที ไม่มีการติดตั้งและไม่เขียนรีจิสทรี
3. กด **Ctrl+O** (หรือ **File > Open...**) แล้วเลือกไฟล์ PDF หรือลากไฟล์ PDF
   หนึ่งไฟล์มาวางบนหน้าต่าง คุณยังเปิด PDF หนึ่งไฟล์โดยตรงตอนเปิดโปรแกรมได้ด้วย:
   `FastPDF.exe "C:\path\to\file.pdf"` (พาธอาจมีช่องว่างและ Unicode ได้ โดยครอบด้วยเครื่องหมายคำพูด)
4. เมื่อต้องการย้ายหรือลบโปรแกรม ให้ย้ายหรือลบโฟลเดอร์นั้นได้เลย

## สิ่งที่รวมอยู่ใน 0.2.0

- **การอ่าน PDF แบบต่อเนื่อง** — จัดวางหนึ่งคอลัมน์ เรนเดอร์หน้านอกเธรด UI ลงเป็น
  bitmap บน CPU แล้วแสดงผลด้วย Direct2D แสดงภาพย่อขนาดครึ่งก่อนแล้วจึงแทนด้วยภาพคุณภาพเต็ม
- **ส่วนติดต่อแบบทันสมัย (Modern UI)** — เปิดใช้ visual styles ของ Windows
  common controls v6 ทำให้เมนู คอนโทรลของแผงค้นหา และแถบเลื่อนใช้ธีม Windows
  ปัจจุบันพร้อมสถานะ hover / focus / pressed และแถบสถานะแสดงชื่อเอกสารด้านซ้าย
  ส่วนเลขหน้า/ซูมชิดขวาด้วยสีหม่นลง
- **Per-Monitor DPI** — หน้าต่าง แถบสถานะ และแผงค้นหา ปรับตาม DPI ของจอที่หน้าต่างอยู่
  และจัดวางใหม่ทันทีเมื่อเปลี่ยน DPI ไม่ใช่เฉพาะตอนเปิดโปรแกรม
- **ดับเบิลคลิกเพื่อ Fit Page** — ดับเบิลคลิกบนเนื้อหาหน้าที่เรนเดอร์แล้วจะย่อหน้านั้น
  ให้พอดีกับกรอบมอง (คำสั่ง Fit Page) ส่วนการคลิกที่ขอบข้างหน้าหรือช่องว่างระหว่างหน้าจะถูกเพิกเฉย
- **การเปิดโดยตรงจาก command line** — ส่งพาธ PDF หนึ่งไฟล์เป็นอาร์กิวเมนต์แรก
  (`FastPDF.exe "path\to\file.pdf"`) รองรับช่องว่างและ Unicode รวมถึงรูปแบบคำสั่ง
  file association ของ Explorer จริง การเรียกโดยไม่มีอาร์กิวเมนต์จะเปิดหน้าต่างว่าง
  ตามปกติและไม่พยายามเปิดตัวโปรแกรมเองเป็น PDF
- **การซูมและ page anchor** — Fit Width, Fit Page, 100%, ซูมกำหนดเองได้ในช่วง
  25%-800%, ซูมด้วย Ctrl + ล้อเมาส์โดยยึดตำแหน่งตัวชี้ และมี page anchor ที่คงตำแหน่งตามเหตุผลไว้ทั้งเมื่อซูม เมื่อปรับขนาดหน้าต่าง และเมื่อเปลี่ยนจอที่มี DPI
  ต่างกัน
- **โหมดนำเสนอ (F11)** — เต็มจอไร้ขอบ จัดแต่ละหน้าพอดีกับกรอบมอง เรนเดอร์หน้าข้างเคียงล่วงหน้าจึงเปลี่ยนหน้าได้เร็วใกล้เคียงศูนย์ และคืนค่าสถานะเดิมทั้งหมดเมื่อออก
- **จับภาพหน้าเอกสาร (Ctrl+S)** — ลากกรอบเลือกที่คร่อมหลายหน้าได้ ภาพประกอบขึ้นจากเนื้อหาหน้าที่เรนเดอร์แล้วเท่านั้น คัดลอกลงคลิปบอร์ดเป็น `CF_DIBV5` และ PNG พร้อมบันทึกเป็นไฟล์ PNG ในขั้นตอนเดียว
- **แปลง PDF เป็น PNG** — เลือกหน้าแบบ All / Current / Custom (ระบุช่วงเช่น
  `1-5, 8, 10`) คุณภาพ Standard (150 DPI) หรือ High (300 DPI) มีแถบความคืบหน้าและยกเลิกได้ ตั้งชื่อไฟล์แน่นอนแบบเติมเลขศูนย์ เช่น `doc_page_001.png` และไม่เขียนทับไฟล์ที่มีอยู่เดิม
- **แปลงภาพเป็น PDF** — แปลง PNG / JPEG / JPG / BMP ได้ทีละหลายไฟล์ จัดลำดับรายการใหม่ได้ เลือกหน้า A4 แนวตั้งหรือแนวนอนอัตโนมัติ คงสัดส่วนภาพ จัดการช่อง alpha อย่างปลอดภัย เขียนผลลัพธ์แบบ atomic และยกเลิกได้
- **พิมพ์แบบ native (Ctrl+P)** — ระบุเครื่องพิมพ์ได้ทั้งระบบและเครือข่าย, ช่วงหน้า,
  ขนาดกระดาษตามที่มีจริงในแต่ละเครื่องพิมพ์พร้อมสำรองเป็น A4, การขยายแบบ Fit หรือ
  Actual Size, ทิศทาง Auto/Portrait/Landscape, จำนวนสำเนา 1-999, ดูตัวอย่างก่อนพิมพ์ด้วยโมเดลการจัดวางเดียวกับที่ใช้ส่งพิมพ์จริง, ส่งพิมพ์บนเธรดพื้นหลังพร้อมยกเลิกได้,
  เรนเดอร์เป็นเวกเตอร์ผ่าน PDFium ลง printer DC และมีตัวสำรองเป็นการเรนเดอร์แบบ raster
  ความละเอียดสูง
- **ค้นหาข้อความแบบทันที (Ctrl+F)** — ค้นบนเธรดพื้นหลังด้วยแผงแบบไม่บล็อก มีตัวนับ `current / total` นำทางด้วย `<` / `>` เก็บผลได้สูงสุด 5,000 รายการ และไฮไลต์กรอบตำแหน่งของผล ช่องข้อความและปุ่ม `<` / `>` / ปุ่มปิดของแผงส่งคำสั่งไปถึงหน้าต่างหลักผ่านตัวส่งต่อคำสั่ง (command forwarder) ที่ติดตั้งบนแผง การพิมพ์และการคลิกจึงสั่งงานการค้นหาได้จริง การเชื่อมต่อนี้ยืนยันด้วย `fastpdf_search_ui_routing_tests` (4 กรณีทดสอบ) และไม่ได้ทดสอบการใช้งานจริงบนหน้าจอด้วยมือ
- **ไฟล์ที่เปิดล่าสุด** — เก็บได้สูงสุด 10 รายการ พร้อมคืนหน้า ตำแหน่งการมอง และระดับซูมเดิม บันทึกไว้ที่ `%LOCALAPPDATA%\FastPDF\recent.dat` และตัดรายการที่อ่านไม่ได้ออกอัตโนมัติ
- **ข้อมูลวินิจฉัย (Ctrl+D)** — ตัวเลขสดบนแถบสถานะ (ความลึกคิวงาน ขนาดและอัตราการพบข้อมูลในแคช และเวลาต่อเฟรม) พร้อมไฟล์บันทึกแบบหมุนเวียนที่
  `%LOCALAPPDATA%\FastPDF\fastpdf.log` (หมุนเมื่อเกิน 1 MB) ไม่มีการบันทึกลายลักษณ์อักษรหรือข้อความในเอกสาร และตัวโปรแกรมไม่มีเส้นทางเรียกใช้งานเครือข่าย
- **ข้อความผิดพลาดที่อ่านเข้าใจ** — ไฟล์ที่หาไม่พบ อ่านไม่ได้ เสีย ตั้งรหัสผ่าน หรือใช้ระบบป้องกันที่ไม่รองรับ จะแสดงข้อความสั้น ๆ ที่ไม่ใช่ศัพท์ทางเทคนิคแทนการค้าง
- **ไอคอนแอปพลิเคชัน** — ไอคอนเอกสาร PDF แบบต้นฉบับที่สร้างจากรูปทรงเชิงเรขาคณิตและฝังอยู่ในไฟล์โปรแกรม ใช้ทั้งใน Explorer แถบชื่อหน้าต่าง แถบงาน และ Alt+Tab

## องค์ประกอบของแพ็กเกจแบบพกพา

        FastPDF.exe                ไฟล์โปรแกรมหลัก
        pdfium.dll                 ไลบรารี PDFium runtime เวอร์ชันตรึง
        THIRD_PARTY_NOTICES.txt    เอกสารสิทธิ์และคำขอบคุณบุคคลที่สาม
        README.txt                 README ของโปรเจกต์
        LICENSE.txt                ข้อความสัญญาอนุญาต/เงื่อนไขจากสคริปต์แพ็กเกจ
        MANIFEST.txt               ค่าแฮช SHA-256 ของทุกไฟล์ในแพ็กเกจ

แพ็กเกจสร้างจากการ build แบบ Release และใช้ `MANIFEST.txt` ตรวจความถูกต้องของไฟล์ด้วย
`Get-FileHash` ได้

## ข้อจำกัดที่ทราบใน 0.2.0

- ไฟล์ PDF ที่ตั้งรหัสผ่านจะแสดง `This PDF requires a password.` — ยังไม่มีกล่องให้กรอกรหัสผ่าน
- การเรนเดอร์ถูกลำดับผ่าน call gate ของ PDFium เพียงชุดเดียว หน้าที่ใช้เวลานานจึงต้องรอหลังงานปัจจุบัน (แต่ UI ไม่บล็อก)
- Fit Width / Fit Page ในโหมดอ่านคำนวณตาม **หน้าแรก** ของเอกสาร
- การค้นหาทำงานกับชั้นข้อความเท่านั้น ไม่มี OCR หน้าสแกนจึงไม่พบผลลัพธ์
- การวางหลายไฟล์พร้อมกันจะส่งเฉพาะไฟล์ภาพที่รองรับไปยัง Image to PDF ส่วนไฟล์ PDF ในการวางแบบหลายไฟล์จะถูกเพิกเฉย
- การพิมพ์ทดสอบผ่าน print spooler ของ Windows และ **Microsoft Print to PDF** ยังไม่ได้ทดสอบกับเครื่องพิมพ์หมึกหรือเลเซอร์จริง
- ไม่มีการรวม PDFium ไว้ใน source repository การ build ต้องใช้อาร์ติแฟกต์เวอร์ชันตรึงที่อธิบายไว้ใน
  [`third_party/pdfium/PDFIUM_LOCK.md`](https://github.com/underoatz-netizen/FastPDF/blob/master/third_party/pdfium/PDFIUM_LOCK.md)
  (release tag `chromium/8035` เวอร์ชัน 154.0.8035.0 รุ่น non-V8)
- repository ต้นทางไม่มีไฟล์ `LICENSE` ที่ root ส่วน `LICENSE.txt` ในแพ็กเกจนี้คือข้อความสัญญาอนุญาต/เงื่อนไขที่สร้างโดยสคริปต์แพ็กเกจ
- ตัวเลขประสิทธิภาพเป็นสิ่งที่สังเกตได้บนเครื่องเดียวจาก `scripts/benchmark_startup.ps1`
  ซึ่งวัดเฉพาะเฟสการเริ่มโปรแกรม/เปิดไฟล์/เฟรมแรกของ FastPDF เอง ตัวเลขเหล่านี้**ไม่ใช่**
  การอ้างว่า FastPDF เร็วกว่า SumatraPDF หรือโปรแกรมดู PDF อื่นใด การเปรียบเทียบข้ามผลิตภัณฑ์
  ต้องใช้วิธีที่ตรงกัน

## การ build จากซอร์ส (กรณีไม่มีไฟล์ binary แนบ)

    cmake --preset release -DPDFIUM_ROOT=C:/deps/pdfium
    cmake --build --preset release
    ctest --preset release
    .\build\release\bin\Release\FastPDF.exe

## สถานะการทดสอบ

CTest ผ่าน 24 รายการเมื่อเปิดใช้ PDFium ทั้งในการ build แบบ Debug และ Release เมื่อ build ด้วย `-DFASTPDF_WITH_PDFIUM=OFF` การตั้งค่าลงทะเบียน 15 รายการในปัจจุบัน แต่ผลการรันครบทั้งชุดที่บันทึกไว้ล่าสุดคือ 13/13 ซึ่งบันทึกไว้ก่อนจะเพิ่มรายการทดสอบการส่งต่อคำสั่งของแผงค้นหาและการเปิดจาก command line จึงยังไม่ได้ยืนยันการตั้งค่านี้ใหม่อีกครั้ง ผลเหล่านี้ผู้ดูแลบันทึกไว้บนเครื่องอ้างอิง Windows 11 x64 เพียงเครื่องเดียว repository นี้ไม่มี workflow CI และ `fastpdf_print_integration_tests` ต้องอาศัย Microsoft Print to PDF บนเครื่องที่รันด้วย

## เอกสารสิทธิ์บุคคลที่สาม

FastPDF เชื่อมกับไลบรารี **PDFium** (BSD 3-Clause, The PDFium Authors / Google LLC) และใช้องค์ประกอบของ Windows SDK (Direct2D, DirectWrite, WIC, Win32 APIs) ดูรายละเอียดใน
[`THIRD_PARTY_NOTICES.md`](https://github.com/underoatz-netizen/FastPDF/blob/master/THIRD_PARTY_NOTICES.md)
อาร์ติแฟกต์ PDFium มีไฟล์ `LICENSE` และโฟลเดอร์ `licenses/` ของตัวเอง ซึ่งต้องติดไปด้วยเสมอเมื่อแจกจ่าย `pdfium.dll`
```

## ข้อควรปฏิบัติสำหรับผู้เผยแพร่

- แนบเฉพาะไฟล์ที่ build และตรวจสอบแล้วจริง ได้แก่ไฟล์ ZIP และถ้าต้องการ จะคัดลอกค่าแฮชจาก
  `MANIFEST.txt` มาวางเป็นข้อความธรรมดาในคำอธิบาย Release ด้วยก็ได้
- อย่าพิมพ์ลิงก์ดาวน์โหลดเอง — GitHub จะสร้างลิงก์ของ asset ให้เมื่อแนบไฟล์
- อย่าประกาศค่าแฮชของ ZIP ล่วงหน้า ให้เผยแพร่ค่าแฮชหลังจากไฟล์แนบมีอยู่จริงด้วยคำสั่ง
  `Get-FileHash .\dist\FastPDF-0.2.0-win-x64.zip -Algorithm SHA256`
- สตริงเวอร์ชันมาจาก `CMakeLists.txt` ควรแก้ที่ไฟล์นั้นก่อน build แพ็กเกจถัดไป เพื่อให้ชื่อ ZIP และเนื้อหาใน `MANIFEST.txt` ตรงกัน
- ให้ commit และ push เอกสารชุดนี้ก่อนเพราะข้อความภาษาไทยในบล็อกด้านบนลิงก์ไปยัง `README.th.md` ซึ่งลิงก์จะเปิดได้บน GitHub ก็ต่อเมื่อเอกสารอยู่ใน repository แล้ว
- ข้อความในบล็อกด้านบนเป็นฉบับภาษาอังกฤษ ให้ใช้
  [RELEASE_NOTES.md](RELEASE_NOTES.md) ประกอบกันสำหรับภาษาไทย

## เอกสารที่เกี่ยวข้อง

- [บันทึกการเผยแพร่ฉบับภาษาอังกฤษ](RELEASE_NOTES.md)
- [README (English)](README.md) · [README (ไทย)](README.th.md)
- [เอกสารสิทธิ์บุคคลที่สาม](THIRD_PARTY_NOTICES.md)
- [สัญญาการ integrate PDFium เวอร์ชันตรึง](third_party/pdfium/PDFIUM_LOCK.md)
- [สคริปต์สร้างแพ็กเกจเผยแพร่](scripts/package_release.ps1)
